# Nixpacks configuration for user-service
[phases.setup]
nixPkgs = ['nodejs']

[phases.install]
cmds = [
  'cd /app/microservices/shared && npm install --production',
  'cd /app/microservices/user-service && npm ci',
  'cd /app/microservices/user-service && mkdir -p node_modules/@flores-victoria',
  'cd /app/microservices/user-service && cp -r ../shared node_modules/@flores-victoria/'
]

[start]
cmd = 'cd /app/microservices/user-service && node src/server.js'

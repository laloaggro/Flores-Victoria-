# Nixpacks configuration for Product Service
# Los comandos se ejecutan desde el Root Directory configurado en Railway

[phases.setup]
nixPkgs = ['nodejs']

[phases.install]
cmds = [
  'npm ci'
]

[start]
cmd = 'node src/server.js'

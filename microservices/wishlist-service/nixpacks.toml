# Nixpacks configuration for wishlist-service
[phases.setup]
nixPkgs = ['nodejs']

[phases.install]
cmds = [
  'cd /app/microservices/shared && npm install --production',
  'cd /app/microservices/wishlist-service && npm ci',
  'cd /app/microservices/wishlist-service && mkdir -p node_modules/@flores-victoria',
  'cd /app/microservices/wishlist-service && cp -r ../shared node_modules/@flores-victoria/'
]

[start]
cmd = 'cd /app/microservices/wishlist-service && node src/server.js'

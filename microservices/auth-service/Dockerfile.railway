# Build version: 3.1.0 - Auth Service with Full Auth Routes
FROM node:18-alpine

WORKDIR /app

# Copy everything first
COPY . .

# Rename package file and install
RUN cp package-simple.json package.json && \
    rm -f package-lock.json && \
    npm install

CMD ["node", "src/server.simple.js"]

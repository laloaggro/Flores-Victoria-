# Build version: 3.0.1 - Auth Service Simplified (Fixed)
FROM node:18-alpine

WORKDIR /app

# Copy solo package-simple.json y renombrarlo
COPY package-simple.json ./package.json

# Install dependencies sin cache issues
RUN npm install --loglevel verbose

# Copy application code
COPY . .

# Railway inyecta PORT dinámicamente
CMD ["node", "src/server.simple.js"]

# Nixpacks configuration for review-service
[phases.setup]
nixPkgs = ['nodejs']

[phases.install]
cmds = [
  'cd /app/microservices/shared && npm install --production',
  'cd /app/microservices/review-service && npm ci',
  'cd /app/microservices/review-service && mkdir -p node_modules/@flores-victoria',
  'cd /app/microservices/review-service && cp -r ../shared node_modules/@flores-victoria/'
]

[start]
cmd = 'cd /app/microservices/review-service && node src/server.js'

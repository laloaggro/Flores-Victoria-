# ============================================
# .env.example para Railway/Producción
# ============================================
# ⚠️  SEGURIDAD: NUNCA usar estos valores en producción
# ⚠️  Generar secrets únicos para cada entorno
# ============================================

# API Gateway
PORT=3000
NODE_ENV=production

# ============================================
# BASE DE DATOS - PostgreSQL
# ============================================
DB_HOST=postgres
DB_PORT=5432
DB_NAME=flores_db
DB_USER=flores_user
# ⚠️ GENERAR PASSWORD SEGURO: openssl rand -base64 24
DB_PASSWORD=<GENERAR_PASSWORD_SEGURO_MIN_24_CHARS>

# ============================================
# AUTENTICACIÓN - JWT
# ============================================
# ⚠️ GENERAR SECRETS: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=<GENERAR_SECRET_64_CHARS_MINIMO>
JWT_REFRESH_SECRET=<GENERAR_OTRO_SECRET_DIFERENTE>
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# ============================================
# BASE DE DATOS - MongoDB
# ============================================
# ⚠️ GENERAR PASSWORD SEGURO: openssl rand -base64 24
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=<GENERAR_PASSWORD_SEGURO>
MONGODB_URI=mongodb://admin:<PASSWORD>@mongodb:27017/products_db?authSource=admin
DISABLE_CACHE=false

# ============================================
# CACHE - Redis
# ============================================
REDIS_HOST=redis
REDIS_PORT=6379
# REDIS_PASSWORD=<OPCIONAL_PASSWORD_SI_REDIS_REQUIERE_AUTH>
REDIS_RATELIMIT_DB=2
REDIS_CACHE_DB=1

# ============================================
# ADMIN PANEL
# ============================================
ADMIN_PANEL_PORT=3021

# ============================================
# SERVICIOS OPCIONALES
# ============================================
# RabbitMQ (Event Bus)
# RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672

# Observabilidad
# JAEGER_AGENT_HOST=jaeger
# JAEGER_AGENT_PORT=6831
# PROMETHEUS_SCRAPE=true

# ============================================
# PASARELAS DE PAGO (usar keys de TEST en desarrollo)
# ============================================
# STRIPE_SECRET_KEY=sk_test_<TU_KEY>
# PAYPAL_CLIENT_ID=<TU_CLIENT_ID>
# PAYPAL_CLIENT_SECRET=<TU_SECRET>

# ============================================
# NOTAS DE SEGURIDAD
# ============================================
# 1. Generar valores únicos para CADA entorno (dev/staging/prod)
# 2. Usar Railway Variables o GitHub Secrets para producción
# 3. Rotar secrets periódicamente (cada 90 días recomendado)
# 4. Nunca commitear .env con valores reales
# Nixpacks configuration for Order Service
# Root Directory already set to microservices/order-service/ in Railway

[phases.setup]
nixPkgs = ['nodejs']

[phases.install]
cmds = ['npm install']

[start]
cmd = 'node src/server.js'

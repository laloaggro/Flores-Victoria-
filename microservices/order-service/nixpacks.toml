# Nixpacks configuration for order-service
[phases.setup]
nixPkgs = ['nodejs']

[phases.install]
cmds = [
  'cd microservices/shared && npm install --production && cd ../order-service && npm ci && mkdir -p node_modules/@flores-victoria && cp -r ../shared node_modules/@flores-victoria/'
]

[build]
builder = "DOCKERFILE"
dockerfilePath = "microservices/contact-service/Dockerfile"
rootDirectory = "microservices/contact-service"
watchPatterns = ["microservices/contact-service/**"]

[deploy]
startCommand = "node src/server.simple.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

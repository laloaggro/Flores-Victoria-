[build]
builder = "DOCKERFILE"
dockerfilePath = "docker/Dockerfile.order-service"

[deploy]
startCommand = "node src/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

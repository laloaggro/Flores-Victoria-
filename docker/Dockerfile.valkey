# Valkey - Open Source Redis Fork
# https://valkey.io/
FROM valkey/valkey:8-alpine

# Configuración optimizada para producción
RUN mkdir -p /data /usr/local/etc/valkey

# Puerto por defecto
EXPOSE 6379

# Volumen para persistencia
VOLUME /data

# Health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
  CMD valkey-cli ping | grep -q PONG || exit 1

# Comando de inicio con configuración básica
CMD ["valkey-server", "--appendonly", "yes", "--protected-mode", "no", "--bind", "0.0.0.0"]
